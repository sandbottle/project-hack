import { AstBlock, Token } from '../common';
export declare class Parser {
    private _currentToken;
    private _moduleName;
    /**
     * Parses tokens and return Ast - Abstract Syntax Tree for jsPython code
     * @param tokens tokens
     * @param options parsing options. By default it will exclude comments and include LOC (Line of code)
     */
    parse(tokens: Token[], name?: string, type?: string): AstBlock;
    private instructionsToNodes;
    private sliceWithBrackets;
    private groupComparisonOperations;
    private groupLogicalOperations;
    private tokensToInstructionLines;
    private createExpressionNode;
}
//# sourceMappingURL=parser.d.ts.map